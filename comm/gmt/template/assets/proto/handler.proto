syntax = "proto3";

package {{.name}};

import "proto/{{.name}}/{{.name}}.proto";

service {{toTitle .name}} {
	rpc {{toTitle .entity}}Add({{toTitle .entity}}AddReq) returns ({{toTitle .entity}}AddRes) {}
    rpc {{toTitle .entity}}Del({{toTitle .entity}}DelReq) returns ({{toTitle .entity}}DelRes) {}
    rpc {{toTitle .entity}}Update({{toTitle .entity}}UpdateReq) returns ({{toTitle .entity}}UpdateRes) {}
    rpc {{toTitle .entity}}One({{toTitle .entity}}OneReq) returns ({{toTitle .entity}}OneRes) {}
    rpc {{toTitle .entity}}Page({{toTitle .entity}}PageReq) returns ({{toTitle .entity}}PageRes) {}
}
